namespace BlazorUniversityCom;

public static class CustomKeys
{
    /// <summary>
    /// Indicates that a search index file should be generated if <c>true</c> (the default).
    /// </summary>
    public const string GenerateSearchIndex = nameof(GenerateSearchIndex);
}
